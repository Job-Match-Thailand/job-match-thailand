import { createApp } from 'vue'
const Vue = createApp({})

import * as Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    user: {}
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
